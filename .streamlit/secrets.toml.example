# Salin ke `.streamlit/secrets.toml` (jangan commit file asli ke Git).
# AUTH_ENABLED = false → aplikasi terbuka untuk demo lokal.

AUTH_ENABLED = false

[users]
admin = { password = "ganti-password-admin", role = "admin" }
analyst = { password = "ganti-password-analyst", role = "analyst" }
